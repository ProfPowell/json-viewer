// SSR stub. The component is a no-op during server-side rendering — the host
// element renders as-is and hydrates with the client module on first load.
// Importing this entry registers a placeholder element if running in a Node-like
// environment so JSX/SSR pipelines don't choke on the custom tag.
export {}
